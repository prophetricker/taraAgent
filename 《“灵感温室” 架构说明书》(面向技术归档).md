# 3.《“灵感温室” 架构说明书》(面向技术归档)

本规范用于指导底层技术选型及 AI 辅助编程 (Vibecoding) 的代码生成，力求高复现性与扩展性。

### 一、 核心技术栈矩阵

| 模块 | 选用技术栈 | 核心职责 |
| :--- | :--- | :--- |
| **前端框架** | Next.js (App Router) + React | 承载全栈 MVP 逻辑，路由与页面渲染 |
| **视觉呈现** | React Flow + Framer Motion | 渲染主线动态拓扑图，处理碎片悬浮物理动效 |
| **AI 调度层** | Vercel AI SDK | 处理流式大模型对话，接管 Function Calling |
| **数据库** | Supabase (PostgreSQL + pgvector)| 统一存储关系型数据(图谱结构)与 Embedding 向量 |
| **大模型层** | Claude 3.5 Sonnet / GPT-4o | 充当苏格拉底导师，进行逻辑推理与信息抽取 |

### 二、 选型理由与避坑指南 (Why & Why Not)

**1. 视觉层：为什么选择 React Flow 而非 Canvas/WebGL (如 PixiJS)？**
* **Why：** 本产品的核心是“文本互动”与“结构化”。React DOM 原生对富文本、输入框、文本选中的支持极其完善。此外，在 Vibecoding 模式下，AI 对 React 及其生态的理解远深于底层绘图 API，代码生成质量极高。
* **Why Not：** Canvas 性能虽强，但处理文本换行、复杂 UI 组件嵌入时心智负担过重，会拉长 MVP 开发周期至不可控。

**2. 数据库：为什么选择 Supabase 而非 Firebase / Neo4j？**
* **Why：** 完美平衡了“开箱即用”与“底层硬核”。它本质是 PostgreSQL，原生支持 `pgvector` 插件。这意味着我们可以在一条 SQL 语句中同时处理灵感节点的关系（父子查询）和语义的相似度计算（向量检索），极其优雅。
* **Why Not Firebase：** NoSQL 结构处理层级脑图极其臃肿，且缺乏原生向量支持，强行使用会导致数据一致性灾难。
* **Why Not Neo4j：** 原生图数据库维护成本太高，MVP 阶段不宜引入双库（图库+向量库）架构。

**3. Agent 路由层：为什么选择轻量级 API 而非 LangGraph 等重型框架？**
* **Why：** 在项目早期，我们需要快速验证“苏格拉底式 Prompt”的效果。Vercel AI SDK 提供无缝的流式输出体验，且 AI 对其原生 Function Calling 语法的生成准确率极高。
* **Why Not LangGraph：** 该框架近期 API 变动过于频繁，AI 极其容易生成夹杂不同版本语法的“缝合怪”代码，导致无尽的 Debug 循环。待产品验证成功、需引入极其复杂的多 Agent 工作流时，再使用 Python 独立重构更为稳妥。

### 三、 架构设计的核心防线
* **绝对的状态分离：** 前端视图层（React Flow）必须是“无脑”的渲染器。一切图谱的增删改，必须由大模型（作为大脑）输出指令，经过后端数据库确认落库后，再单向驱动前端页面更新。禁止前端在未落库的情况下擅自改变数据流。
* **Serverless 连接池约束：** 在 Next.js 的 Serverless 架构下，严禁直连数据库，必须通过 Supavisor 等连接池中间件进行调度，防止高并发导致的连接耗尽。
