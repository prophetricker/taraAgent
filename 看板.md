# 灵感温室 MVP 看板

## 当前重点

右脑画布正在从“碎片散列”改成“主题中心 + 蒲公英轨道 + 明确连线”的结构。当前最重要的验收标准是：新捕捉一枚蒲公英后，画布中心周围必须出现可见的小节点，并通过柔和虚线连到当前主题。

## 正在做

- [x] 当前主题作为布局锚点，保留拖动后的数据库位置，并通过 `fitView` 视觉居中。
- [x] 历史主题默认最多显示 5 个，并退到背景位置，减少旧碎片噪音。
- [x] 蒲公英按确定性弧形轨道围绕当前主题排布。
- [x] 新增蒲公英后主动 `fitView`，避免节点生成在视口外。
- [x] 拖动主题后乐观更新本地画布，让蒲公英轨道跟随主题位置。
- [x] 右上角卡片降级为最近捕捉浮层，只展示最近 3 条。
- [x] 回滚快照目录从 Vitest 和 TypeScript 扫描范围中排除。

## 待验证

- [ ] 本地运行 `npm.cmd test`，确认只执行 `src` 下测试，且全部通过。
- [ ] 本地运行 `npm.cmd run build`，确认生产构建通过。
- [ ] 手动捕捉新蒲公英后，画布中能看到蒲公英小节点。
- [ ] 蒲公英小节点通过柔和虚线连接到中心主题，线旁显示“旁支灵感”。
- [ ] 新碎片顶部弱提示出现后自动消失。
- [ ] 旧历史主题不再铺满画布，只保留少量弱化痕迹。
- [ ] 拖动中心主题后，蒲公英轨道应跟随移动；刷新后位置保持。

## 风险和缺口

- 碎片标题仍是规则清洗，不是真正的语义归纳；后续应让 AI 在自动/手动捕捉时生成 `title` 和 `summary` 字段。
- 蒲公英节点暂不支持独立拖动和持久化；当前由系统围绕主题自动排布。
- 旧数据库里已存在的重复/低质量碎片不会自动删除，需要用户确认后再执行清理。
- 更完整的节点详情面板、碎片编辑/删除、Prompt 行为评测仍未完成。

历史完成项见：`已完成事项清单.md`。
