# 🚀 灵感保护 Agent MVP (v1.0) 需求文档

## 1. MVP 核心目标 (The "Must-Haves")
* **跑通灵感的“捕获 - 存放 - 深挖”全链路。**
* **验证 Agent 的双态心流**（倾听模式与苏格拉底追问模式）是否真的能提供无压力的探索体验。
* **暂缓开发**：无限伸缩的复杂动态拓扑图（模块 C 降级）、外部资讯联网检索（模块 D 砍掉）、复杂的多平台导出 API（模块 E 降级）。

## 2. 核心功能拆解与技术要求

### 模块 A：基础蒲公英生态与 UI 框架 (MVP 级)
MVP 阶段的重点是实现“状态的分离”，视觉表现可以适度降级，但交互逻辑必须清晰。

* **1. 极简花海入口 (Dandelion Board)**
    * **需求描述**：启动应用后，呈现一个平面的“灵感看板”。无需复杂的 3D 物理引擎，可以用随机错落分布的“文字卡片”或“气泡”来代替蒲公英。
    * **功能点**：支持点击气泡直接进入该灵感的专属对话工作流。
* **2. 双栏工作台 (Dual-Pane Workspace)**
    * **左栏（主输入区）**：核心的 Agent 对话流。
    * **右栏（侧边暂存区）**：这是 MVP 版的“无限画布”。暂时不做复杂的树状/网状节点连线，而是做成一个**“局部蒲公英收集栏”**。
* **3. 碎片化输入 (Quick Capture)**
    * **需求描述**：提供一个常驻的全局输入框（或者简单的移动端网页入口），允许用户输入一句话，直接生成一个“野生蒲公英”存入花海，不强制要求立刻开始对话。

### 模块 B：Agent 大脑与核心 Prompt 机制 (MVP 级)
这是整个 MVP 的灵魂，主要依靠优良的 Prompt Engineering 和基础的 Function Calling 来实现。

* **1. 双态心流切换 (State Toggle)**
    * **倾听模式 (Listener)**：System Prompt 设定为“极简确认”。Agent 接收用户的长篇大论，只做简短的语气词回应（如“嗯”、“继续说”），**绝对不主动总结归纳**。
    * **追问模式 (Socrates)**：System Prompt 设定为“发散引导者”。Agent 基于上文，提出一个打破常规视角的“好问题”（例如：“如果不考虑成本，这个想法最极端的形态是什么？”）。
    * **交互方式**：在输入框旁边放置一个明显的物理/视觉 Toggle 开关，由用户手动决定 Agent 当下的状态，降低 AI 误判带来的压迫感。
* **2. 意图识别与自动摘录 (Function Calling)**
    * **需求描述**：当处于对话中时，Agent 需要在后台实时运行一个微型判断模型。
    * **触发条件**：如果 Agent 识别到用户说出了一句“偏离当前主线，但很有价值的想法”，调用工具将其自动提取，化作一张“卡片”推送到右侧的“局部蒲公英收集栏”中。

### 模块 C & E：基础闭环支撑 (降级方案)
为了让 MVP 能够闭环，我们需要极简的记忆和导出。

* **记忆存储 (Basic Memory)**：采用最基础的对话 ID 绑定机制。每个蒲公英气泡绑定一段完整的历史对话记录 (Context History)。暂时不需要做到段落级的向量化检索 (Embedding)，只要能点开气泡接着聊就行。
* **温和出口 (Simple Export)**：提供一个“一键复制全文”或“生成 Markdown 大纲”的按钮，供用户手动粘贴到 Notion 等外部工具即可。

---

## 3. MVP 阶段的开发里程碑建议

| 阶段 | 核心任务 | 技术侧重点 |
| :--- | :--- | :--- |
| **Phase 1** | **核心对话与双态心流验证** | 调优大模型 System Prompt；实现手动切换“倾听/追问”模式。 |
| **Phase 2** | **蒲公英剥离机制** | 跑通 Function Calling，让 Agent 能在长对话中准确抓取旁支灵感并推送到侧边栏。 |
| **Phase 3** | **花海入口与数据绑定** | 构建卡片式的“灵感看板”前端；实现气泡与特定对话历史的数据库绑定。 |

