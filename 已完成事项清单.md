# 已完成事项清单

## MVP 第一阶段

- Supabase 数据架构：`inspiration_nodes`、`dandelion_fragments`、`conversations`、`messages`、RLS、UUID、时间戳、`vector(1536)`。
- Supabase Auth：邮箱 OTP 登录、SSR session、中间件刷新、未登录跳转。
- 左侧对话：AI SDK 流式对话、Markdown 渲染、消息落库、刷新后恢复历史。
- 核心 Prompt：倾听/追问模式、禁止主动 To-Do List、低压灵感保护语气。
- 手动碎片：圈选文本、补充标题、写入碎片、右侧提示和展示。
- 自动碎片：移除关键词兜底，改为保守语义判断、源文本校验、置信阈值和去重。

## 右脑画布

- React Flow 节点语义统一为 `dandelion / extension / fragment`。
- UI 名词统一为“蒲公英中心 / 延伸 / 碎片”。
- 碎片不再与当前中心画常驻连线，来源改为悬停显示。
- 右上角入口改为“碎片池”，支持隐藏全部、显示最近 3 个、逐个显示/隐藏。
- 右下角最近捕捉卡片已移除，避免遮挡和重复表达。
- React Flow 自定义节点已补 Handle，修复 `Couldn't create edge for source handle id: "null"`。
- 关系线样式已准备：推导、联想、支撑、冲突、类比、待判定。
- 本轮新增浮动关系线，线从节点边缘连接，而不是从中心点连接。
- 本轮新增短标题、一句话归纳、标签提取与标签悬停高亮。
- 本轮新增关系驱动的延伸布局和外围碎片布局。

## 工程保障

- `看板.md` 只记录当前重点、待验证和待做。
- `概念词典.md` 记录蒲公英、延伸、碎片、关系线等统一概念。
- `vitest.config.ts` 限定只执行 `src` 下测试，避免旧快照污染测试集。
- `tsconfig.json` 排除 `rollback-snapshots`，避免旧快照污染类型检查。
