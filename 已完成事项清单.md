# 已完成事项清单

## MVP 第一阶段已跑通

- Supabase 数据架构：`inspiration_nodes`、`dandelion_fragments`、`conversations`、`messages`、RLS、UUID、时间戳、`vector(1536)`。
- Supabase Auth：邮箱 OTP 登录、SSR session、中间件刷新、未登录跳转。
- 左侧对话：AI SDK 流式对话、Markdown 渲染、消息落库、刷新后恢复历史。
- 核心 Prompt：倾听/追问模式、禁止主动 To-Do List、低压灵感保护语气。
- 手动蒲公英：圈选文本、补充标题、写入碎片、右侧提示和展示。
- 自动蒲公英：移除关键词兜底，改为保守语义判断、源文本校验、置信阈值和去重。

## 右脑画布已完成能力

- 当前主题节点和历史主题节点分层显示。
- 历史主题弱化显示，点击可高亮并展开摘要。
- 主题节点可拖动，位置保存到 `position_x / position_y`。
- 蒲公英碎片可投射到 React Flow 画布。
- 蒲公英连线使用柔和虚线，并标注“旁支灵感”。
- 新碎片出现时，画布顶部显示弱提示。
- 右上角卡片降级为最近捕捉浮层，不再作为主要结构。

## 工程保障

- `看板.md` 记录当前任务。
- `待验证.md` 记录用户侧验收项。
- `rollback-snapshots/2026-04-28-before-autonomous-run/` 提供非 Git 状态下的手动回滚快照。
- `vitest.config.ts` 限定只执行 `src` 下测试，避免回滚快照污染测试集。
- `tsconfig.json` 排除 `rollback-snapshots`，避免快照污染类型检查。
