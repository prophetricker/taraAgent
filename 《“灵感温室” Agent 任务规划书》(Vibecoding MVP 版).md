# 1.《“灵感温室” Agent 任务规划书》(Vibecoding MVP 版)

本规划书采用自底向上（Bottom-up）的策略，将庞大的系统拆解为 AI 辅助编程环境下极易实现的单点任务。

### 第一阶段：MVP 核心路径跑通 (高优)

**任务 1：初始化基础设施与数据架构**
* **目标：** 建立基于 Supabase 的 PostgreSQL 数据库，并配置向量支持。
* **验收标准：**
    * 成功创建 `inspiration_nodes` (包含父子层级与 `vector` 字段) 和 `dandelion_fragments` 表。
    * 时间戳 (`created_at`)、默认 UUID 等约束全部生效。
    * 在 Next.js 项目中成功配置 Supavisor 数据库连接池与 ORM/SQL 客户端。

**任务 2：前端主框架与“右脑视觉”画布渲染**
* **目标：** 使用 Next.js 搭建带双栏布局的前端，并引入 React Flow。
* **验收标准：**
    * 左侧为对话占位区，右侧为支持鼠标无限拖拽缩放的画布。
    * 前端能向数据库请求静态测试数据，并在 React Flow 中正确渲染出连线的节点。

**任务 3：构建“左脑对谈”流式交互层**
* **目标：** 接入 Vercel AI SDK，实现极其丝滑的打字机对话体验。
* **验收标准：**
    * 前端能够发送消息，并流式接收大模型的文本回复，无明显卡顿。
    * 实现前端简单的 Markdown/富文本渲染能力，保证文字排版的可读性。

**任务 4：注入“灵感守护者”核心大脑 (Prompt Engineering)**
* **目标：** 将预设好的“状态机”与“内部思维链” System Prompt 写入 API 路由，塑造 Agent 的苏格拉底式性格。
* **验收标准：**
    * 在不触发工具的情况下，Agent 能够根据用户输入的成熟度，在“共鸣发散”和“扎根收敛”状态间自然切换。
    * Agent 绝不主动输出 To-Do List，而是用提问引导用户。

**任务 5：实现“双手”功能 - 自动化蒲公英提取 (Function Calling)**
* **目标：** 让 Agent 在对话后台自主调用 `create_dandelion_fragment` 工具。
* **验收标准：**
    * 用户在聊天框输入大段游离的发散想法时，Agent 的文本回复保持连贯，同时右侧画布边缘自动通过 Framer Motion 动效浮现出带摘要的“蒲公英碎片”。
    * 数据准确写入 Supabase 的碎片表。

**任务 6：用户主动圈选与快捷提取机制**
* **目标：** 实现低打扰的手动灵感捕捉。
* **验收标准：**
    * 用户用鼠标在左侧聊天流中高亮一段文本，弹出迷你输入框。
    * 输入补充想法后点击确认，不影响主对话流，右侧立即生成对应碎片。

### 第二阶段：后续完善与架构演进 (长期规划)

*(注：此阶段为保持敏捷，仅作方向性规划)*
* **记忆回溯引擎 (RAG)：** 实现长下文的定时切片与向量压缩，让 Agent 能在几周后准确召回历史碎片，打造真正的长期认知外脑。
* **多端输入流构建：** 开发 iOS/Android 的快捷指令入口，支持语音转文字快速倾倒碎片至收件箱。
* **微服务大脑剥离：** 将 Agent 逻辑从 Next.js 中抽离，使用 Python/FastAPI 结合 LangGraph 重构，引入更复杂的多专家协同 (MoE) 和思维图谱推理。

