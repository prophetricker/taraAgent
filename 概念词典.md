# 灵感温室概念词典

## 核心对象

- `蒲公英`：一个已经相对成形的想法图。它包含中心话题、延伸节点和关系线。
- `中心话题`：某个蒲公英的核心节点。用户当前正在聊的主线通常就是当前蒲公英的中心话题。
- `延伸`：某个蒲公英内部继续长出的想法节点，也就是蒲公英的冠毛。延伸可以有层级，也可以和其他延伸交叉连接；更新的延伸通常由旧延伸支撑，位置可以更靠外。
- `碎片`：远离当前话题、暂时还没形成蒲公英的游离想法。碎片是另一个蒲公英的前身。
- `成长为蒲公英`：碎片被补全、聚合、命名后，升级成新的蒲公英。
- `关联`：两个蒲公英或两个延伸有关，但还不够合并为同一个蒲公英时，用关系线连接。
- `合并`：后来发现两个蒲公英其实是一类，才合成一个蒲公英。合并比关联更强。

## 关系线

- `推导`：A 导致 B，或 B 是 A 的逻辑后果。使用较粗实线。
- `联想`：A 让人想到 B，但不是严格逻辑。使用较细虚线。
- `支撑`：B 是 A 的例子、证据、材料。使用绿色细实线。
- `冲突`：B 反驳、拉扯、挑战 A。使用红褐色虚线。
- `类比`：A 和 B 的结构相似。使用蓝灰色点线。
- `来源`：碎片从某个蒲公英对话中被捕捉出来。来源只在悬停碎片时显示，不画成常驻关系线。
- `待判定`：系统或用户暂时不确定关系。使用浅灰细线。

## 当前实现映射

- `inspiration_nodes` 暂时承载蒲公英中心和延伸节点。
- `dandelion_fragments` 暂时承载碎片。表名历史遗留，UI 和工程语义中统一称为“碎片”。
- React Flow 中的 `dandelion` 节点代表蒲公英中心。
- React Flow 中的 `extension` 节点代表蒲公英延伸。
- React Flow 中的 `fragment` 节点代表碎片。

## 设计原则

- 不再把碎片叫蒲公英。
- 不再把所有节点强行排成轨道。
- 当前画布应该是可连接的图结构。
- 延伸默认显示在当前蒲公英图中，不作为第一优先隐藏对象；布局应由关系和可读性驱动，不强制做同心圆或轨道。
- 碎片默认进入碎片池，用户可以隐藏或取回。确认关系后，碎片才能成为延伸、成长为蒲公英，或与其他蒲公英建立关联。
